name: health_care_appointment_app
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=2.18.0-165.1.beta <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  easy_image_viewer: ^1.0.4
  paginated_search_bar: ^1.1.2
  drop_down_list: ^0.0.2
  google_nav_bar: ^5.0.6
  line_icons: ^2.0.1
  percent_indicator: ^4.0.1
  datepicker_dropdown: ^0.0.5
  animated_custom_dropdown: 1.2.2
  image_picker: ^0.8.5+3
  path_provider: ^2.0.10
  flutter_switch: ^0.3.2
  modal_progress_hud_nsn: ^0.2.1
  cool_stepper: ^1.2.1

  cupertino_icons: ^1.0.2
  validation_extensions: ^1.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:

  uses-material-design: true

  assets:
    - assets/